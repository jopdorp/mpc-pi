# Test aid: keep the loaded project playing so the audio path can be heard.
#
# A drop-in rather than an edit to the unit, so "make it play for testing" and
# "what the appliance does on power-on" stay separate and either can be removed
# without touching the other. Delete this file for instrument behaviour.
[Service]
Environment=MPCPI_AUTOPLAY=1
