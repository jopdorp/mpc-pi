diff --git a/src/osd/sdl/osdsdl.cpp b/src/osd/sdl/osdsdl.cpp
index d279e374..6d38c0df 100644
--- a/src/osd/sdl/osdsdl.cpp
+++ b/src/osd/sdl/osdsdl.cpp
@@ -706,6 +706,9 @@ void sdl_osd_interface::process_events()
 
 void sdl_osd_interface::osd_exit()
 {
+	// The main thread must stop touching SDL's video device before the
+	// frontend thread tears down windows and their dynamically-loaded backend.
+	set_external_event_pump_ready(false);
 	osd_common_t::osd_exit();
 
 	shutdown_sdl_video();
@@ -736,7 +739,6 @@ void sdl_osd_interface::shutdown_sdl_video()
 		return;
 	}
 
-	set_external_event_pump_ready(false);
 	std::unique_lock<std::mutex> lock(m_external_event_mutex);
 	m_external_event_state = external_event_state::QUIT_REQUESTED;
 	m_external_event_condition.notify_all();
